services:
  ev1ia:
    image: ev1ia
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ["sh", "-c", "pip install debugpy -t /tmp && python /tmp/debugpy --wait-for-client --listen 0.0.0.0:5678 Doc_Unimarc.py "]
    ports:
      - 5678:5678
    env_file:
      - .env
